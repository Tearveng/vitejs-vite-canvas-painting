import { LinterPluginBase, LinterPluginOptions } from "./linterPlugin";
export declare const buildPluginName = "vite-plugin-linter-build";
export interface LinterBuildPlugin extends LinterPluginBase {
    lintFolder(folder: string): Promise<string[]>;
}
export default function linterPluginBuild(options: LinterPluginOptions | undefined, fileFilter: (id: string | unknown) => boolean): LinterBuildPlugin;
