export declare function normalizePath(id: string): string;
export declare function onlyUnique(value: string, index: number, self: string[]): boolean;
export declare function readAllFiles(folder: string, filter: (fileName: string) => boolean): string[];
