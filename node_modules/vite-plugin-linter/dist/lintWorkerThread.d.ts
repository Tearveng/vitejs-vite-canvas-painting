/// <reference types="node" />
import { Worker } from "worker_threads";
import Linter, { LinterResult } from "./Linter";
import { LinterResultData } from "./linterPlugin";
export interface WorkerThreadMessage {
    files: string[];
    linterName: string;
    result: {
        build?: readonly LinterResultData[];
        serve?: LinterResult<LinterResultData>;
    };
}
export declare function createWorkerThreads(command: "build" | "serve", pluginName: string, linters: Linter<LinterResultData>[]): {
    [linterName: string]: Worker;
};
