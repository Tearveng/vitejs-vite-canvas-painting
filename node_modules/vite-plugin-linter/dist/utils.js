"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readAllFiles = exports.onlyUnique = exports.normalizePath = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
function normalizePath(id) {
    return path_1.default.relative(process.cwd(), id).split(path_1.default.sep).join("/");
}
exports.normalizePath = normalizePath;
function onlyUnique(value, index, self) {
    return self.indexOf(value) === index;
}
exports.onlyUnique = onlyUnique;
function readAllFiles(folder, filter) {
    let files = [];
    const children = fs_1.default.readdirSync(folder, { withFileTypes: true });
    for (const child of children) {
        const childName = folder + "/" + child.name;
        if (child.isDirectory()) {
            files = files.concat(readAllFiles(childName, filter));
        }
        else if (filter(childName)) {
            files.push(childName);
        }
    }
    return files;
}
exports.readAllFiles = readAllFiles;
